# Only the deployable site, the publications toolchain, and the
# contributing guide are public.
# Everything else in this workspace (governance, planning, archives,
# incoming artifacts) stays local.
/*
!/.gitignore
!/.gitattributes
!/CONTRIBUTING.md
!/site/
!/tools/

# Build artifacts inside the whitelisted directories.
__pycache__/
*.pyc
