# Only the deployable site and the publications toolchain are public.
# Everything else in this workspace (governance, planning, archives,
# incoming artifacts) stays local.
/*
!/.gitignore
!/.gitattributes
!/site/
!/tools/

# Build artifacts inside the whitelisted directories.
__pycache__/
*.pyc
