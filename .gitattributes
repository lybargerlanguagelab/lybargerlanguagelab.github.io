# The workspace lives on OneDrive under Windows, so working-tree files pick
# up CRLF. Normalize to LF in the repository so a file that was only opened,
# not edited, does not show up as a whole-file diff.
* text=auto eol=lf
*.png binary
*.jpg binary
*.svg text eol=lf
